# hako 设计说明

> 第一版，证据更新至 2026-08-30。本文不是功能宣传页，而是设计辩护记录：每项都说明问题、选择、被否决方案、代价、验证和踩坑。数字只写已经运行得到的结果；未实现能力明确标注边界。

## 项目定位与核心主张

`hako` 是一个从零实现的 Coding Agent，提供命令行与本地 Web 控制台。用户给出编程任务后，主 Agent 调用大模型决定下一步，再由本地内核校验并执行文件或命令工具，把结果放回历史，直到任务完成或触发明确的失败条件。

本项目的目标不是证明“模型会写代码”，而是解决更接近真实软件工程的问题：模型可能输出格式错误、读取旧版本、只分析不行动、修改后不测试、在错误解释器里验证，甚至为了通过测试引入越界补丁。我的核心主张是：

1. **模型是不可信的计划者，不是内核。** 它负责提出工具调用；参数、权限、路径、执行结果和完成条件由本地代码决定。
2. **完成是一种可验证状态，不是一句话。** “我修好了”不能让任务成功，最后一次修改之后必须有新的结构化验证证据。
3. **功能取舍由失败案例和数据驱动。** 有真实需求才增加机制；没有收益证据的 compaction、工具并发不为了展示而实现。

面向实际产品，可以把它理解成一名受控的本地修复工程师：接收 issue、代码和日志，完成“调查 -> 最小修改 -> 本地验证 -> 留下证据”，而不是只在聊天框里给一段建议代码。

## 题目约束如何落到实现

| 题目要求 | hako 的对应实现 |
|---|---|
| 不使用 Agent 框架或服务端托管工具 | 运行时只直接依赖 `openai` 和 `rich`；循环、历史、工具、解析、权限、终止、错误处理均在本仓库实现 |
| 自行管理对话历史与上下文 | `hako/history.py`、`hako/truncate.py`：消息配对、陈旧读取失效、带恢复指针的截断 |
| 自行定义和本地执行工具 | `hako/tools/`：手写 schema、参数校验、路径边界、文件编辑、命令执行和副作用报告 |
| 自行解析模型输出 | `hako/llm.py`：tool call 归一化、有限 JSON 修补、缺失 call id 补全、`finish_reason` 保留 |
| 自行实现循环终止与错误处理 | `hako/loop.py`：分层停止原因、卡死检测、Verified Finish、截断续跑、可恢复工具错误 |
| API key 不得入库 | `hako/config.py` 只从环境变量或被忽略的 `.env` 读取；公开 `.env.example` 不含真实值 |
| 演示真实编程任务 | 本地 PromoOps 仓库固定一个发布缺陷和一次产品迭代；每轮都可重置，并由 Agent 不可见的 held-out tests 独立复核 |

## 总体结构与信任边界

```text
用户任务
   |
   v
Agent 主循环 ---- messages + tool schemas ----> LLM
   ^                                             |
   |                                             v
对话历史 <---- 结构化 ToolResult <---- tool_calls / text
                         |
                         v
              参数校验 -> 权限 -> 本地执行
                         |
                         +--> 文件读取/编辑
                         +--> shell 命令与净副作用审计

主循环 -- typed events --> inline TUI
                         --> headless 评测订阅者
```

信任由外向内逐步收紧：用户指定工作区；模型输出首先只是待校验数据；工具只能通过注册表调用；文件工具必须留在工作区；有副作用操作经过审批；shell 结果以退出码和文件净变化回传；最终由状态机而不是模型自述判定成功。

## 决策索引

| 编号 | 决策 | 主要代码 |
|---:|---|---|
| #1 | 内核与呈现通过同步事件总线解耦 | `hako/events.py` |
| #2 | 分层终止、Verified Finish 与截断续跑 | `hako/loop.py`、`hako/llm.py` |
| #3 | 工具 schema 与契约手写 | `hako/tools/base.py` |
| #4 | 陈旧读取立即失效，长输出截断但可恢复 | `hako/history.py`、`hako/truncate.py` |
| #5 | 模型输出全链路按不可信输入处理 | `hako/llm.py`、`hako/tools/__init__.py`、`hako/tools/base.py` |
| #6 | shell、解释器、权限与文件副作用在执行边界收口 | `hako/tools/shell.py`、`hako/fs_audit.py`、`hako/ui/renderer.py` |
| #7 | 当前工具保持串行，不为不可见收益引入并发 | `hako/loop.py` |
| #8 | Prompt 是独立、可版本化的工程产物 | `hako/prompt.py` |
| #9 | TUI 使用 inline transcript，并手写跨平台读键 | `hako/ui/renderer.py`、`hako/ui/keys.py` |
| #10 | 局部编辑采用唯一匹配 search-replace | `hako/tools/files.py` |
| #11 | 评测采用可重置模板、隐藏测试和外部复核 | 本地 `promoops-demo/`（主仓库外） |
| #12 | subagent 只负责隔离的只读调查 | `hako/subagent.py` |
| #13 | Web 将连续对话建模为 Session，将单条目标建模为 Run | `web/backend/.../SessionService.java`、`web/worker/main.py`、`web/frontend/.../useSessionController.ts` |

## #1 事件总线：内核不渲染

**问题。** 如果主循环直接 `print` Rich 组件，交互演示、无界面评测和未来其他前端会各自复制一套控制流；UI 编码异常还可能把任务一起拖垮。

**决策。** 内核只同步发出 frozen dataclass 事件，例如 `TurnStarted`、`ToolCallFinished`、`ContinuationRequired` 和 `RunFinished`。TUI 与 runner 都是订阅者。订阅者异常不会终止 Agent，但第一次异常会通过独立的 stderr 通道报告，避免静默失败。

**被否决方案。** 一是直接在循环里渲染，否决原因是内核和 UI 无法独立测试；二是异步事件队列，当前循环本身顺序执行，异步只会增加事件乱序、关闭和异常传播成本，没有用户收益。

**代价。** 多一层事件类型和分派代码；新增内核状态时通常也要新增事件和渲染分支。

**验证。** `tests/test_loop.py` 验证事件能到达订阅者，也验证订阅者抛异常时任务仍能完成并留下 failure 计数。

**踩坑。** Windows cp936 不能编码部分图标时，渲染器曾连续抛 `UnicodeEncodeError`；总线为了保护内核吞掉异常，结果整个 transcript 静默消失。修复不是让 UI 异常炸掉任务，而是“一次警告 + UTF-8 设置 + ASCII 字形退路”。

**答辩一句话。** 事件总线的价值不是炫技，而是确保“评测运行的内核”和“视频里展示的内核”是同一套逻辑。

## #2 终止协议：模型说结束，不等于任务已完成

**问题。** 单一的“没有 tool call 就结束”会混淆四种情况：只读任务真的答完、代码改完且测试通过、代码改完但未验证、回复因 token 上限截断而没来得及行动。只设最大步数也无法及时阻止重复调用烧 token。

**决策。** 主循环使用分层停止原因：`DONE_READ_ONLY`、`DONE_VERIFIED`、`DONE_UNVERIFIED`、`INCOMPLETE`、`MAX_STEPS`、`STUCK`、`DENIED` 和 `ERROR`。同一工具参数重复到第三次判定无进展；第二次先给提示，允许一次合理重试。

修改状态遵循：

```text
无业务修改                     -> 可 DONE_READ_ONLY
edit/write/shell 净业务文件变化 -> DIRTY
DIRTY + 验证失败               -> 仍 DIRTY，并清空旧成功证据
DIRTY + 验证成功               -> VERIFIED
VERIFIED + 再次修改            -> 回到 DIRTY
VERIFIED + 模型结束            -> DONE_VERIFIED
```

验证证据来自 `run_command` 返回的结构化 `verification_kind/command/exit code`，不是模型说“测试已通过”。只接受单一、实际执行的测试、构建或静态检查；`pytest --collect-only`、`pytest || true`、管道和命令拼接不计入。注册表覆盖 Python/Node/Rust/Go/.NET/Java 的常见测试与构建，也覆盖 `g++/clang++/cl/javac/cmake --build/make/ninja/msbuild`，但不会因为任意命令退出码为 0 就把它包装成验证。

shell 产生的路径再分成“业务修改”和“派生产物”：源码、配置和脚本会使状态回到 `DIRTY`；`.exe/.class/.jar/.o` 以及 `build/target/out` 等编译输出仍被审计和展示，但不会把刚建立的成功构建证据反向清空。分类保持保守，例如不会把整个 `bin/` 目录一概视为生成物。

对于无工具回复，`LLMClient` 保留 API 的 `finish_reason`。若为 `length/max_tokens`，或兼容端点未返回原因但 completion usage 已撞上限，内核最多两次要求模型停止纸上分析并调用具体工具；仍截断则 `INCOMPLETE`，不伪装成功，也不无限续跑。

**被否决方案。** 一是完全相信模型的最终文本，无法抵抗自我感觉良好；二是只用 `max_steps`，卡死时止损太晚；三是任何修改后强制固定跑 `pytest`，不同项目可能使用其他测试、构建或静态检查；四是截断后无限续写，会造成不可控 token 消耗；五是把编译器生成的二进制也当成人工改动，会形成“构建成功 → 生成 `.exe` → 证据立刻失效”的自相矛盾。

**代价。** 修改后可能多一个模型决策；验证命令注册表和派生产物分类都可能漏掉合法的新工具链。当前选择允许显式扩展并用回归测试固定语义，宁可明确 `DONE_UNVERIFIED`，也不把可疑成功包装成完成。

**验证。** 单元测试覆盖验证前结束、失败验证、验证后再次写入、后续失败清空旧证据、交互任务状态重置、截断后恢复行动以及连续截断返回 `INCOMPLETE`。另有 C++ 回归固定 `write qsort.cpp → g++ 生成 qsort.exe → 模型结束` 必须得到 `DONE_VERIFIED`，并验证 `bin/release.ps1` 仍属于业务修改。截断续跑的因果结论只来自确定性测试，不用一次真实运行的偶然轨迹代替机制证明。

**踩坑。** 最初 `ModelReply` 丢弃了 `finish_reason`，主循环无法区分正常停止和输出截断。另一个坑是“曾经测试通过”不等于“最终版本测试通过”，但“编译命令生成二进制”也不等于 Agent 又改了源码；如果只看 `touched_paths` 而不区分 `authored_paths/derived_paths`，Verified Finish 会在正确的 C++ 流程中误判失败。

**答辩一句话。** Verified Finish 不证明代码绝对正确，只证明“最后一次改动之后，确实有一项认可的验证成功”。

## #3 工具契约：schema 手写，不从类型注解反射

**问题。** 工具 schema 不只是参数类型，还承担行为约束，例如“编辑前先读”“路径相对工作区”“多匹配时不要猜”。自动反射函数签名无法表达这些对模型最重要的信息。

**决策。** 每个 `Tool` 显式声明名称、自然语言描述、JSON Schema、handler、只读标志、审批需求、危险检查和单任务调用上限。注册表自行检查必填字段、转换可恢复的整数类型，并丢弃模型常加的无关解释字段。

**被否决方案。** 一是从注解自动生成 schema，描述贫弱且容易把 Python 实现细节暴露给模型；二是引入 Pydantic 或 Agent SDK，只为少量字段增加运行依赖，同时削弱“重要逻辑自行实现”的可解释性；三是参数稍有偏差就终止任务，会浪费整个模型回合。

**代价。** handler 签名和 schema 存在少量重复，变更参数时必须同时维护；因此测试必须覆盖缺字段、类型不符和多余字段。

**验证。** 注册表测试验证缺失必填项会作为工具错误回传、字符串整数可转换、未知字段不会传入 handler、未知工具不会崩溃。

**踩坑。** OpenAI 消息协议要求每个 assistant tool call 都有对应 tool 消息。即使参数解析失败或用户拒绝，也必须补一条失败结果，否则下一轮请求会被 API 拒绝。

**答辩一句话。** 手写 schema 的重点不是少用一个库，而是把工具的“使用协议”而非只有“类型签名”交给模型。

## #4 上下文管理：消除歧义优先于盲目压缩

**问题。** 同一文件的新旧两个版本若同时留在上下文，模型可能用旧内容构造 `edit_file.old_text`，导致定位失败并重复猜测。另一方面，大文件和长命令输出不能无限占用上下文，但直接截断会让信息不可恢复。

**决策。** 文件工具在边界返回规范化路径。成功写入后，历史里更早的同路径 `read_file` 结果立即替换成占位符，要求需要时重新读取。文件读取按行分页并给出下一次 `offset`；长命令输出保留头尾，中间省略处给出重跑建议。

**被否决方案。** 一是什么都不做，依赖模型自己辨认版本，真实轨迹证明它经常注意不到；二是每次编辑前内核强制重读，虽然正确，但无条件增加工具调用和 token；三是只在上下文快满时处理，混淆“消除版本歧义”和“节省空间”两个不同问题；四是立即实现通用 compaction，会把原始证据改写成模型摘要，引入路径和条件丢失风险。

**代价。** 若任务确实需要比较旧版本，模型必须从 Git diff 或重新读取中恢复；头尾截断也可能省掉中间关键信息，因此省略位置必须可恢复。

**验证。** 测试故意用 `./a.py` 读取、`a.py` 写入，确认下一轮旧内容消失；也验证写后新读不会被误伤、分页指针和头尾截断存在。当前 PromoOps 连续任务没有出现 context-length 错误，也没有达到预设的 70% compaction 门槛，因此不为未发生的问题增加压缩机制。

**踩坑。** 如果只比较模型原始路径字符串，`./a.py` 和 `a.py` 会让失效机制静默失灵。修复是由工具通过 `subject_path/touched_paths` 声明规范化后的真实对象。

**答辩一句话。** 陈旧读取失效解决的是正确性歧义；compaction 解决的是容量问题，不能因为名字都叫上下文管理就混成一个机制。

## #5 不可信模型输出：有限修补，边界再校验

**问题。** 兼容端点的 tool arguments 实际出现过 Markdown fence、尾随逗号、括号未闭合、双重 JSON 编码和缺失 call id。即使 JSON 合法，模型仍可能少参数、给错类型、越过工作区或调用不存在的工具。

**决策。** 解析层只做可解释的有限修补：去 fence、去尾随逗号、补齐明显未闭合括号、解一次双重编码；修不成就生成带 `parse_error` 的调用，不执行，并把错误作为 tool result 回传。之后注册表再做 schema 校验，文件工具在实际 I/O 前 `resolve()` 并拒绝越过 workspace 的相对路径、绝对路径和符号链接逃逸。

**被否决方案。** 一是任何 JSON 错误都终止任务，会把可恢复格式问题升级成致命故障；二是使用过度宽松或递归猜测的修补器，可能把模型本来没表达的操作“修”出来并执行；三是只在 prompt 里提醒路径边界，提示词不是安全边界。

**代价。** 有限修补也可能误解模型意图，因此修补后仍必须得到 dict 并经过完整参数、权限和路径校验；无法证明时宁可失败重试。

**验证。** JSON repair、注册表、路径逃逸和符号链接测试分别覆盖解析层、契约层与文件系统边界。网络错误只对 429、timeout 和服务端错误做有限指数退避；明确 4xx 直接报告，避免用重复请求掩盖 key、模型名或消息结构错误。

**踩坑。** “解析成功”不是“允许执行”。最初容易把 JSON 修补、参数校验和路径校验写成一个模糊的 try/except；拆成三层后，错误归因和面试解释都更清楚。

**答辩一句话。** 模型输出从进入客户端到落到磁盘，至少经过语法、schema、权限和路径四道边界。

## #6 执行与权限：把环境歧义和副作用收口到工具边界

**问题。** Coding Agent 必须执行项目命令，但 shell 既需要管道和重定向能力，又可能删除文件、推送远端或绕过文件工具；Windows 的 PATH 还可能让 `pytest.exe` 与启动 hako 的 Python 来自不同环境。

**决策。** `run_command` 不使用 `shell=True` 拼接 argv，而是把模型给出的整条命令作为 PowerShell/sh 的单个参数，内部可信操作则使用纯 argv。stdin 连接空设备，交互式和常驻命令提前拒绝，超时后返回可恢复失败。

Windows 上仅当命令明确以 bare `pytest/pytest.exe` 开头时，实际执行改为当前 `sys.executable -m pytest`；显式解释器、其他测试器和非 Windows 命令保持原样。验证元数据保存真实执行命令。

权限分三层：只读工具无需批准；普通写入和命令默认询问，可由 `-y` 或会话记忆放行；递归删除、Git push/reset/clean、网络下载、系统/磁盘破坏和 DROP 等高风险命令必须逐次人工确认，检查发生在所有快捷放行之前。非交互环境普通操作需要显式 `-y`，高风险操作即使有 `-y` 也拒绝；库调用未传审批策略时默认拒绝有副作用工具。

shell 前后对工作区做快照，区分新增、修改和删除，并合并进 `touched_paths`；再把编译二进制、字节码和常见构建目录标为 `derived_paths`。全部副作用都会进入事件与审计结果，只有 `authored_paths = touched_paths - derived_paths` 会推进修改状态。版本库内部状态、虚拟环境、依赖、`tmp` 和常见测试缓存在遍历入口剪枝；小于 1MB 的业务文件额外做 BLAKE2 摘要，大文件比较 metadata。失败或超时前留下的业务文件仍会进入 DIRTY 状态。

**被否决方案。** 一是完全移除 shell，Agent 无法安装、测试、构建和检查；二是 `-y` 真正放行一切，隔离评测方便但产品边界不可接受；三是只靠危险字符串拒绝而不做审批分级，无法区分普通命令和需要逐次确认的外部/破坏性操作；四是声称工作区快照等同操作系统沙箱，这是错误安全承诺。

**代价。** 危险检测是保守词法门禁，可能误报且不是完整 PowerShell/sh AST；快照增加固定开销，并只能观察命令结束时仍存在的净变化。当前仓库初版全量扫描约 2.5s，剪枝生成目录后扫描 43 个业务文件，单次约 17.85ms；该数字不外推到其他仓库。

**验证。** 权限测试证明 headless 无 `-y` 拒绝普通副作用、`-y` 和 remembered `run_command` 不能覆盖高风险命令、库默认策略拒绝写工具。真实子进程测试一次产生 `files +1 ~1 -1`，缓存不计入；脚本先写文件再超时，失败结果仍报告该文件；C++/Java 构建命令和派生产物分类由独立测试覆盖。

PromoOps 彩排曾暴露解释器错位：直接执行 PATH 中的 `pytest` 无法导入目标仓库依赖。修复后，Windows 上的 bare pytest 由启动 hako 的当前 Python 环境执行，真实命令仍进入结构化验证证据。

**踩坑。** 快照若遍历 ignored `tmp/` 中多份回退和评测副本，安全机制本身会给每条命令增加秒级延迟；必须在遍历入口剪枝，而不是读完后再过滤。另一个坑是 timeout 不能只抛异常，否则命令超时前留下的半成品会逃出 `ToolResult`。

**答辩一句话。** 我没有声称 shell 已经安全；我把“能否执行、实际执行了什么、工作区净变了什么”分别变成可测试的边界。

## #7 工具调度：当前保持串行

**问题。** 同一轮可能包含多个只读工具调用，看起来可以并发；但并发会让事件顺序、异常归属、读取与后续写入依赖变复杂。

**决策。** 当前 `_execute_calls` 串行执行全部工具。预先设定只有“可并发只读工具至少占总墙钟 10%，且理论可节省 1 秒”才实现并发；写工具和验证命令始终保持顺序。

**被否决方案。** 一是看到 `read_only` 标记就立即 `gather`，它只能优化本地 I/O，不能缩短主导耗时的模型请求；二是把 pytest 与写入并发，会破坏“验证发生在最后一次修改之后”的因果顺序。

**代价。** 在高延迟远程文件系统或一次读取很多大文件时，串行可能损失墙钟时间；若未来数据越过门槛，应只并发互不依赖的只读调用，并明确事件排序和异常聚合规则。

**验证。** 当前真实运行的主要等待来自模型请求与测试命令，本地目录和文件读取没有成为可感知瓶颈，也没有越过预先声明的并发门槛。因此这里只保留可复现的串行语义，不把未经测量的并发当卖点。

**踩坑。** 早期注释曾写“只读可并发、写入串行”，但循环实际上完全串行。文档描述不存在的能力比少一个功能更危险，因此最终选择诚实记录“不实现及其数据理由”。

**答辩一句话。** 并发不是越多越先进；当可优化部分不到 0.1% 时，确定顺序和可复现性更值钱。

## #8 Prompt：行为约束独立版本化

**问题。** Prompt 会直接改变模型是否先读文件、是否小步验证、是否使用 subagent。如果埋在 CLI 或循环的 f-string 里，就无法定位行为变化，也容易重复工具 schema 浪费每轮 token。

**决策。** 系统提示单独放在 `hako/prompt.py`，只描述行为原则和环境：先读后改、局部编辑、验证要求、工具失败恢复、平台/shell，以及启用时的只读委派条件。精确参数留在 tool schema，不重复两遍。

**被否决方案。** 一是巨型提示词列出所有参数和失败案例，每轮重复付费且容易与代码漂移；二是只依赖模型常识，不同提供商和操作系统行为不稳定；三是把安全只写在 prompt，模型提示不是强制边界。

**代价。** Prompt 改动可能让真实模型轨迹变化，单元测试无法完全预测；因此重要措辞需要在冻结任务上做单变量对照，不能用一次漂亮运行作普遍结论。

**验证。** 配置关闭 subagent 时 schema 和提示均不存在委派内容；开启后才注入可观察的使用条件。Windows 环境会明确告诉模型 shell 类型，而 bare pytest 的真正一致性仍由工具代码保证，不依赖它记住提示。

**踩坑。** 只写“复杂任务可以委派”过于模糊；即使补充可观察条件，也不能为了得到一次漂亮的采用结果无限重跑。

**答辩一句话。** Prompt 负责引导，内核负责强制；把两者混在一起会让安全和实验都无法解释。

## #9 TUI：保留可回看的 inline transcript

**问题。** Coding Agent 的调试经常要回看二十步前读了什么、哪个命令失败。全屏 TUI 退出后可能丢失 scrollback；引入大型终端框架也会增加题目之外的依赖。

**决策。** Rich 只负责 inline 追加渲染；思考 spinner 可以原地刷新，但工具轨迹保留在终端历史。方向键菜单由 Windows `msvcrt` 和 POSIX `termios/tty` 的小型实现完成，非 TTY 明确退化。控制台优先配置 UTF-8，不支持时使用 ASCII 字形。

**被否决方案。** 一是 Textual 等全屏框架，视觉更强但丢失自然 transcript 且依赖更重；二是 `input()` 编号菜单，简单但每次批准都需要回车和额外文本；三是第三方 readchar/questionary，核心能力只有几十行且非 TTY 退化行为不一定符合评测。

**代价。** inline UI 不适合复杂多窗格、鼠标和任务树；手写键盘处理必须负责恢复 POSIX 终端状态。

**验证。** Windows 和 Linux CI 跑同一套核心测试；事件订阅者异常不会杀死任务。审批策略通过无 TTY、普通确认、会话记忆和高风险逐次确认的确定性测试。

**踩坑。** POSIX raw mode 若异常后不在 `finally` 恢复，用户 shell 会失去回显；Windows 非 UTF-8 管道则需要 ASCII 退路。

**答辩一句话。** TUI 的首要产品目标是让工程过程可追溯，不是把 CLI 做成更花哨的全屏应用。

## #10 编辑协议：唯一匹配的 search-replace

**问题。** Agent 需要局部修改已有文件。整文件重写消耗 token、容易改变无关格式；unified diff 依赖行号和上下文格式；简单 `str.replace(..., 1)` 又可能在重复片段中悄悄改错位置。

**决策。** `edit_file(path, old_text, new_text)` 只在 `old_text` 唯一匹配时落盘。0 匹配返回接近候选并要求重读；多匹配列出行号并要求增加上下文；绝不静默选择第一处。read_file 输出统一为 LF，但编辑只适配定位串和替换片段到原文件 CRLF，不重写全文件换行；GBK 等已有编码尽量保留，新文本不可编码时在写盘前拒绝。

**被否决方案。** 一是 unified diff，模型生成的行号容易随上下文漂移，解析和部分应用语义更复杂；二是整文件重写，最小修改不可证明；三是自动模糊匹配并取最高分候选，错误位置的“成功写入”比显式失败更危险；四是多匹配默认取第一个，无法证明编辑意图。

**代价。** 模型必须先读文件并提供足够上下文；文件刚变化时 old_text 会失败，需要重读，多一次工具调用换取可解释安全。

**验证。** 测试覆盖 0/1/多匹配、重叠匹配、空定位串、候选提示、CRLF 最小 diff、GBK 保留、不可编码拒绝、路径逃逸，以及编辑后旧读取失效与重新验证。

**踩坑。** Python 在 Windows 默认换行转换可能让整个文件变成 diff；另一个坑是先截断原文件再发现编码失败。实现先在内存生成并编码完整结果，成功后才一次写盘。

**答辩一句话。** search-replace 不是最通用的补丁格式，但它用“唯一性”把模型意图变成了一个可检查前置条件。

## #11 评测：不信任被测 Agent 的自述

**问题。** 一段录屏只能证明某次运行看起来成功；Agent 自己跑的测试也可能使用错误解释器、修改测试文件或越过任务范围。反复重跑直到成功还会造成选择偏差。

**决策。** 主仓库外的本地 `promoops-demo/` 保存不可直接操作的阶段模板，并把模板复制到 `work/` 供 Agent 修改。Run1 从含发布缺陷的模板开始，Run2 从功能迭代基线开始；每轮先证明基线事实，再运行 Agent。结束后从 Agent 工作区之外执行 held-out tests，独立确认业务结果，而不是复述 Agent 自己的结论。

硬分同时要求：

```text
原始缺陷可复现
+ Agent 为 DONE_VERIFIED
+ 公开测试通过
+ 隐藏测试通过
+ 确有源码修改
+ 禁改文件未变化
+ 所有最终变化位于白名单
```

**被否决方案。** 一是只看 Agent 最后一次 pytest 的退出码，它无法排除测试被改弱或覆盖不足；二是让 Agent 自己总结改了什么，不能替代外部复核；三是把隐藏测试放入工作区，模型可能针对答案修改；四是失败后静默重试并覆盖旧结果，会产生挑选偏差。

**代价。** 当前只有两个阶段的单次彩排，不能计算成功率或统计显著性；隐藏测试提高可信度，但仍只覆盖设计者想到的边界。

**验证。** Run1 基线为 `1 failed, 26 passed`，修复后公开测试 `27 passed`，外部 held-out `9 passed`；Run2 完成基线公开测试 `36 passed`，外部 held-out `6 passed`。这些数字分别说明缺陷修复、产品迭代和外部复核，不能相加成一个“总通过数”。

**踩坑。** 测试环境错误不能冒充代码失败；公开测试和 held-out tests 也不能混为一个数字。另一个坑是一次通过不能写成“成功率提升”，因此文档只记录这次可复现彩排的事实。

**答辩一句话。** Agent 内部测试回答“它为什么认为自己修好了”，runner 独立复核回答“评测者是否应该相信它”。

## #12 只读 subagent：隔离调查，不分散写入责任

**问题。** 复杂仓库调查可能同时涉及日志和多个实现文件，主上下文会被调查材料占用；但让多个 Agent 同时写代码会引入冲突、责任不清和验证顺序问题。

**决策。** 可选 `delegate_readonly` 创建独立短上下文子 Agent，只注册 `list_dir/read_file`，没有 shell、写工具或递归委派。每个主任务最多调用一次；子模型的真实 token 计入父任务总成本。返回内容明确标记为“未经测试的调查备忘录”，主 Agent 保留唯一写入权和 Verified Finish 责任。

**被否决方案。** 一是允许子 Agent 写补丁，无法维持单写入者和最终责任；二是与主 Agent 共享完整历史，不能达到隔离调查噪声的目的；三是递归 subagent，成本和终止难以控制；四是强制所有复杂任务调用，会给不需要的任务增加模型费用。

**代价。** 多一次模型调用，备忘录本身仍可能错误；主 Agent需要重新核对，不能直接当补丁或测试证据。若模型不自然采用，功能存在也不产生产品收益。

**验证。** 确定性权限测试确认子模型每轮只收到 read/list schema、第二次委派被拒、文件前后不变、token 纳入总量。这证明隔离边界存在，但不能证明真实任务中一定会自然采用或带来收益。

**踩坑。** 机制存在不等于产品收益已经成立；继续重跑直到出现一次采用会变成挑选结果。因此当前结论只能是“隔离机制可用，自然采用率和收益尚未证明”，子 Agent 输出也不能天然当成正确答案。

**答辩一句话。** subagent 是隔离的调查员，不是第二个可以随便改仓库的主工程师。

## #13 Web 会话：Conversation 跟 Session 绑定，不跟单条目标绑定

**问题。** 早期 Web 把一次 prompt、一个 Worker 和一条时间线都叫 task。这样能跑单次演示，却无法自然表达“先定位 → 再修复 → 最后补测试”；如果每次追问都新建 Agent，模型会丢掉前文。如果取消一次运行就杀 Worker，用户也不能继续纠正方向。另一方面，直接把所有操作永久塞进一个全局 Agent，又会让不同仓库任务互相污染。

**决策。** 显式区分四个对象：Workspace 是工具操作目录；Session 是一次连续工程对话；Run 是用户在 Session 中发送的一条目标；Attachment 是某个 Run 增加的 user context。普通取消只结束 Run 并保活当前 Worker；切换或新建会话时，当前 Session 进入 `SUSPENDED`，Worker 退出但 Session 身份和可展示历史保留。再次发送后续目标时，后端以同一个 sessionId、新 workerId 启动 Worker，只把已完成 Run 的“用户输入 + 最终回答”恢复为语义 Conversation，然后创建新 Run。旧 tool call、文件读取、stdout/stderr 不恢复，因为它们描述的是过去的工作区状态；新 Agent 必须重新读取当前仓库。Session 与 Run 使用两套状态机；事件必须携带 sessionId，Run 事件再携带 runId；后端与前端双重丢弃迟到身份。

**被否决方案。** 一是“一 prompt 一 Worker”，实现简单但没有真正多轮上下文；二是把 Cancel 等同于关闭 Session，用户无法在取消跑偏后继续追问；三是让输入框 `+` 同时承担附件和新会话，两个完全不同的生命周期会混淆；四是完整重放事件或 Python 对象，旧工具观察会污染当前仓库状态，LLM SDK、进程和精确 token 计数也无法可靠序列化；五是所有历史 Worker 永久驻留，少量会话虽可承受，但把产品正确性绑定到进程不退出，重启后仍会失忆；六是多 Session 并发，当前单用户本地答辩不需要，却会扩大进程、审批和 Workspace 冲突面。

**代价。** 后端必须维护两套状态机、Run/Session 级事件身份、挂起/恢复协议和事件游标；取消命令时只能杀本次进程树，不能误杀仍需保活的 Worker。语义重建保留工程意图和结论，但不声称恢复原进程、旧工具观察、精确 token 统计或模型隐藏状态；恢复后的第一次调查可能重复读文件。SQLite 现在会保存 prompt、附件文本、最终回答与工具事件，因此它是本地敏感数据，必须留在被忽略目录。当前上限为 200 条语义消息，符合本地少量会话定位，不是无限历史方案。

**验证。** Spring 子进程集成测试同时覆盖两条路径：同一 OPEN Session 的第二 Run 复用 workerId；SUSPENDED Session 恢复后 sessionId 不变、workerId 改变、runCount 递增，Fake Worker 仍能引用上一轮语义上下文。Python 测试确认只接受完整的 user/assistant 交替消息，拒绝悬空或乱序历史，并确认恢复内容不含 tool 消息。等待审批时取消后 Session 仍可继续；旧 Worker 退出后历史事件仍可查看。前端测试确认左侧列表不暴露 OPEN/CLOSED 等内部枚举，历史会话可直接进入继续输入。

**踩坑。** 最初把输入框 `+` 误解成新会话入口，后来拆成附件与侧栏“新会话”。第二个坑是把历史事件重画出来误当作 Conversation 恢复；事件适合审计，只有成对的用户语义和最终回答适合重建模型上下文。第三个坑是 Windows 中只 kill PowerShell 父进程会让 Python/Java 子进程继续持有 stdout，界面虽显示取消但命令仍跑；修法是独立进程组 `CTRL_BREAK`，再以 `taskkill /T /F` 兜底。

**答辩一句话。** Run 是一句新目标，Session 才是一段连续工程记忆；挂起只回收 Worker，恢复时重建语义 Conversation，但仓库事实必须重新读取，不能把旧工具日志冒充当前状态。

## 证据边界：已验证、推断与尚未证明

### 已验证事实

- 当前本地核心回归：Python `203 passed, 1 skipped`，Spring Boot `18 passed`，前端 `34 passed`；生产构建转换 `324` 个模块。本次未提交改动尚未获得远端 CI 结论。
- 真实 V4-Flash 最小闭环已完成 `list/read/edit/pytest/DONE_VERIFIED`；单例只能证明链路可工作。
- PromoOps Run1 有稳定失败基线、修复后公开测试和外部 held-out 复核；Run2 有产品迭代后的公开测试和外部 held-out 复核。
- Windows bare pytest 会统一到当前解释器；截断续跑、Verified Finish 和错误终止由确定性测试覆盖。
- shell 真实子进程的新增、修改、删除和超时残留会进入 `touched_paths`；权限快捷放行不能覆盖高风险命令。
- compaction 和只读工具并发均未越过预先设定的数据门槛。

### 基于数据的推断

- 两轮 PromoOps 彩排证明同一 Agent 可以先修复真实缺陷、再继续产品迭代，但样本量不足以代表任意仓库成功率。
- 当前任务中模型/API 延迟远大于本地只读工具耗时，因此优化只读并发不会改善用户可感知墙钟。
- 只读 subagent 能产出有价值证据，但目前没有自然采用率或性能收益证据。

### 尚未证明或明确不承诺

- Verified Finish 不证明测试覆盖充分、代码无隐藏缺陷或形式化正确。
- shell 文件快照不是进程/网络/容器沙箱，看不到工作区外和同一命令内已抵消的瞬时副作用。
- 正常 `finish_reason=stop` 但任务语义实际需要修改时，内核没有通用意图分类器；当前只对可观测的截断误结束做强约束。
- 当前没有统计意义上的任务成功率、模型排名或 subagent 消融收益。
- 没有实现通用 compaction、只读工具并发或可写多 Agent；重新评估条件见下表。

| 未实现项 | 当前不实现的证据 | 重新评估条件 |
|---|---|---|
| 通用 compaction | 当前连续任务无 context-length 错误，也未达到 70% 门槛 | 达到窗口 70%，或真实任务被历史膨胀阻断 |
| 只读工具并发 | 当前观察中本地读取不是用户可感知瓶颈 | 可并发读取占比 >=10%，且理论可省 >=1s |
| 可写 subagent | 单写入者更易归因和验证；只读采用尚未证明 | 出现可独立分区、冲突可检测且有重复样本收益的任务 |
| 完整 OS 沙箱 | 当前实现是权限门禁和工作区观测，不具备系统隔离能力 | 进入不可信仓库产品化部署时接入容器/受限进程边界 |

## PromoOps 两轮如何组成产品故事

两轮不是互不相关的算法题，而是同一个运营仓库持续演进的两个阶段：

1. **Run1：真实线上 Bug。** 运营保存并发布新优惠规则后，接口提示成功，但线上结算仍读取旧版本。Agent 必须沿 API、服务和仓储持久化路径定位遗漏字段，做局部修复并用回归测试证明线上版本真正切换。
2. **Run2：真实产品迭代。** 在修复后的仓库上继续增加可调整 Priority 与同优先级冲突提示。Agent 需要理解既有领域模型、API、页面和测试，在同一 Session 中完成跨层开发与验证。

产品叙事可以概括为：**真实 Bug 修复的难点不是生成一段补丁，而是控制证据、修改范围、执行权限和完成标准。** hako 的差异化不在工具数量，而在于把一次修复变成可回放、可拒绝、可验证的工程闭环。

## 面试高频追问的短答案

**为什么不用 LangChain/Agent SDK？** 题目要求重要逻辑自行实现；更重要的是，循环、历史、权限和终止正是我要展示的设计对象。只使用厂商 API 客户端，不把关键决策交给框架黑盒。

**为什么不是工具越多越好？** 每个 schema 每轮都占 token，语义重叠还会让模型选错。核心只保留高频且需要结构化边界的能力，其他操作由受控 shell 兜底。

**为什么不让模型自己判断完成？** 模型知道的是自己的叙述，不知道内核是否真的写盘、退出码是否为 0、验证之后是否又修改。完成必须由执行证据和状态机共同决定。

**为什么 search-replace 而不是 diff？** 当前任务里，唯一定位串是一个更小、更容易测试的正确性条件；0/多匹配明确失败，比自动套错 patch 更安全。

**权限层是不是安全沙箱？** 不是。它是默认拒绝、逐次确认和净副作用观测；真正不可信代码执行仍需要操作系统或容器沙箱。

**为什么做了 subagent 却不把它当卖点？** 物理只读和机制可用已经证明，但两次自然任务没有采用，缺少产品收益证据。诚实的负结果也是设计判断的一部分。

**为什么没做 compaction 和并发？** 先设门槛再看数据：上下文不到 1%，可并发本地 I/O 不到 0.1%。此时实现只会增加新失败面，无法改善当前任务。

## 下一版文档需要补什么

第一版已经覆盖当前源码的主要设计。进入最终提交前还需要：

1. 给每个决策补最终稳定的代码行号或 commit 链接，避免仓库继续变化后锚点失效。
2. 把 README.txt 的 1000 汉字版本与本文件核对，确保特色功能和限制不矛盾。
3. 视频冻结后，把最终演示任务、命令、结果 JSON 和两分钟时间轴写入附录。
4. 若后续改变任何机制，先增加可复现实验和失败证据，再更新结论；不把计划写成已实现事实。
