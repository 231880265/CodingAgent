# Product

## Register

product

## Users

hako 面向在本机代码仓库中处理工程任务的开发者，以及需要演示、审查或答辩 Agent 工作过程的使用者。用户关心的不是与模型闲聊，而是快速确认任务现在进行到哪一步、Agent 准备执行什么副作用、修改是否经过验证、失败能否追溯。

## Product Purpose

hako 是一个通用 Coding Agent，可完成 Bug 修复、小型功能开发、局部重构和补充测试。Web 控制台把现有 CLI 的“调查、受控修改、执行验证、可信结束”呈现为可观察、可审批的单任务工作流。成功不是页面显示一段乐观结论，而是用户能看到变更路径、停止原因和最后一次修改之后的结构化验证证据。

## Brand Personality

冷静、精确、可信。文案像一套成熟工程工具：短句、具体、不过度拟人，不使用夸张承诺，也不把模型思考包装成系统事实。界面应让用户感到控制权始终在自己手中。

## Anti-references

- 不做以聊天气泡为中心的 AI 助手，也不使用机器人、魔法、星光或“智能涌现”意象。
- 不使用霓虹渐变、玻璃拟态、发光边框、满屏指标卡等通用 AI/SaaS 模板。
- 不模仿完整 IDE，不在 MVP 中塞入文件树、代码编辑器、模型市场或复杂设置。
- 不把每条事件包装成同尺寸卡片，不用颜色单独表达成功、失败或风险。
- 不展示虚假的实时思考、成功率、性能提升或未由内核提供的 Before/After 证据。

## Design Principles

1. **证据先于叙述。** 模型文字明确标为说明，测试、退出状态和 Verified Finish 单独呈现。
2. **当前动作最重要。** 任务状态、未决审批和取消入口始终比历史细节更容易找到。
3. **逐层展开。** 默认显示工具摘要、结果和耗时，源码片段与完整输出按需展开，失败自动展开。
4. **安全边界可见。** 批准按钮旁说明操作对象、风险和影响；取消明确提示不会自动回滚已有修改。
5. **诚实处理不完整。** 未验证、断线缺口、Worker 故障和缺少 baseline 都使用准确状态，不用模糊的“已完成”掩盖。

## Accessibility & Inclusion

以 WCAG 2.1 AA 为基础目标。正文和代码保持足够对比度；状态同时使用文字、图形和颜色；所有表单、折叠、审批与取消操作支持键盘；焦点清晰可见；尊重 reduced-motion；中文路径、长命令和窄屏场景不得造成关键操作不可达。
